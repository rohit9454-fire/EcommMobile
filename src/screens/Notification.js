import React from 'react';
import { Text, SafeAreaView } from 'react-native';

const Notification = () => {
    return (
        <SafeAreaView style={{ justifyContent: 'center', alignItems: 'center', flex: 1, backgroundColor: 'red' }}>
            <Text>Notification</Text>
        </SafeAreaView>
    )
}

export default Notification;